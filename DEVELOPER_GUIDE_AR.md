# قصة بناء هذا الموقع: دليل المطور المبتدئ 📘

أهلاً بك يا صديقي!
طلبت أن تعرف كيف بنينا هذا الموقع من الصفر. سأحكي لك القصة خطوة بخطوة، وسنستخدم صفحة "المتجر" (Shop) كمثال عملي لنفهم كيف تحولت الأفكار إلى أكواد.

---

## الفصل الأول: البداية (الأرضية) 🏗️

لم نبدأ بكتابة ملفات فارغة. في البرمجة الحديثة، نستخدم أدوات تجهز لنا "ورشة العمل".
استخدمنا أداة اسمها **Vite** لإنشاء المشروع.
لماذا؟ لأنها تجهز لنا ملفات Vue الأساسية وتربطها ببعضها فوراً.

ثم اخترنا **Vue.js** كإطار عمل.
**السبب:** تخيل أنك تريد تغيير سعر منتج في 10 أماكن في الصفحة.

- بالطريقة القديمة: يجب أن تبحث عن الـ 10 أماكن وتغيرها يدوياً بالكود.
- مع Vue: أنت تغير "الرقم" في مكان واحد (المخزن)، وVue يغيره في الـ 10 أماكن تلقائياً!

---

## الفصل الثاني: بناء صفحة المتجر (Structure) 🏠

دعنا ننظر لملف `src/views/ShopView.vue`. هذا الملف هو صفحة المتجر كاملة.
عندما بدأنا، كان الملف فارغاً. فكرنا: "ماذا نحتاج؟"

1.  **صندوق بحث**.
2.  **قائمة فلاتر** على اليسار.
3.  **شبكة منتجات** على اليمين.

### كيف كتبنا هذا؟ (HTML & Bootstrap)

استخدمنا **HTML** لبناء الهيكل، و **Bootstrap** للتنسيق السريع.

```html
<!-- استخدمنا Row لعمل صف، وقسمناه لعمودين -->
<div class="row">
  <!-- العمود الأول: القائمة الجانبية (يأخذ 3 وحدات من 12) -->
  <aside class="col-lg-3">...</aside>

  <!-- العمود الثاني: المنتجات (يأخذ 9 وحدات من 12) -->
  <main class="col-lg-9">...</main>
</div>
```

**لماذا Bootstrap؟**
لاحظ `col-lg-3`. هذه الكلمة السحرية تقول للمتصفح: "اجعل هذا العمود يأخذ ربع الشاشة". بدلاً من كتابة 50 سطر CSS، كتبنا كلمة واحدة.

---

## الفصل الثالث: الروح والمحرك (JavaScript) 🧠

هنا السحر الحقيقي. هذا ما كتبناه في قسم `<script setup>`. سأشرح لك **لماذا** كتبنا كل سطر.

### 1. المتغيرات التفاعلية (`ref`)

أردنا مكاناً لتخزين المنتجات التي ستظهر.
في الجافاسكريبت العادي، قد تكتب: `let products = []`.
لكن في Vue، كتبنا:

```javascript
const products = ref([])
```

**لماذا `ref`؟**
لو استخدمنا المتغير العادي وجاءت البيانات من السيرفر، المتصفح لن يعرف أن البيانات تغيرت، وستظل الشاشة بيضاء!
كلمة `ref` تخبر Vue: "راقب هذا المتغير جيداً!! إذا تغيرت قيمته، أعد رسم الشاشة فوراً ليرى المستخدم المنتجات الجديدة".

### 2. جلب البيانات (`async` / `await`)

نحن لا نملك المنتجات على جهازنا، هي موجودة في قاعدة بيانات (Firebase) على الويب.
لذلك كتبنا دالة لجلبها:

```javascript
// async تعني أن هذه العملية ستأخذ وقتاً (ليست فورية)
const fetchProducts = async () => {
    // await تعني: "انتظر هنا ولا تكمل الكود حتى تصل البيانات"
    const snapshot = await getDocs(collection(db, 'products'))
    products.value = snapshot.docs.map(...)
}
```

**لماذا `async/await`؟**
بدونها، الكود سيحاول عرض المنتجات قبل أن تصل من الإنترنت أصلاً، وسيحدث خطأ. هذه الكلمات تجعل الكود "صبوراً".

### 3. البحث الذكي (`computed`) 🔍

أضفنا صندوق بحث. كيف نجعل البحث فورياً؟
استخدمنا خاصية رائعة تسمى `computed` (المحسوبة).

```javascript
const filteredProducts = computed(() => {
  // 1. خذ قائمة المنتجات الأصلية
  // 2. هل كتب المستخدم شيئاً في البحث؟
  if (searchQuery.value) {
    // نعم! إذن قم بتصفية القائمة وأبقِ فقط المنتجات التي يطابق اسمها البحث
    return products.value.filter((item) => item.name.includes(searchQuery.value))
  }
  // لا؟ أعد القائمة كاملة
  return products.value
})
```

**لماذا؟**
هذا ليس مجرد دالة عادية. `computed` ذكي جداً؛ هو يعرف أنه يعتمد على `searchQuery`.
بمجرد أن يكتب المستخدم حرفاً واحداً، `computed` يشعر بذلك، ويعيد تشغيل الفلتر، ويعطي النتيجة الجديدة لشبكة المنتجات.

---

## الخلاصة: كيف تفكر كمطور؟ 💡

عندما بنينا هذا المشروع، لم نكتب الكود دفعة واحدة.

1.  **رسمنا الشكل**: (نحتاج صفحة فيها منتجات وبحث).
2.  **بنينا الهيكل**: (HTML + Bootstrap).
3.  **أضفنا البيانات**: (متغيرات `ref` فارغة).
4.  **ربطنا البيانات**: (جلبنا البيانات الحقيقية من Firebase).
5.  **أضفنا التفاعل**: (خاصية `computed` للبحث).

هكذا بنينا Rennaya Shop، وهكذا يمكنك بناء مشروعك القادم! 🚀
